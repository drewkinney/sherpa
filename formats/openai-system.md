keywords: openai
best_for: GPT API system/user split, Cursor/Windsurf/Antigravity system prompts, most non-Claude IDE copilots

Two distinct blocks labeled SYSTEM: and USER:. System block carries role, persona, constraints, and output format — the durable instruction layer. User block carries the specific task/request only — the one-off ask. Mirrors the system/user message role split rather than a single unified block.
