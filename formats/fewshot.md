keywords: fewshot, few-shot, few shot
best_for: classification/extraction tasks, fine-tuning-style prompts, pattern-matching tasks

Brief instruction framing, followed by 2-3 input→output example pairs illustrating the desired pattern, followed by the actual task as a final unfilled input. If the source prompt doesn't contain enough information to construct real examples, ask the user for one or two before proceeding — never invent examples from nothing, since fabricated examples actively mislead this format.
