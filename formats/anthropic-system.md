keywords: system, anthropic
best_for: Claude API system parameter, Claude Projects custom instructions

Prose-XML hybrid following role → context/background → task → output format → examples, matching the structure Anthropic's own prompt engineering documentation recommends (docs.claude.com prompt-engineering/overview). Reads like a system prompt meant to sit above a conversation persistently, not a one-off user message. Heavier on framing and persona than the raw XML format.
