keywords: xml
best_for: Claude system prompts, Claude Code CLAUDE.md, tool/function definitions

Open/close tags for each populated slot — <role>, <context>, <task>, <constraints>, <output>, <examples> as needed. Omit tags for slots that don't apply to this prompt rather than including empty ones. Nest freely if a slot has sub-parts (e.g., multiple constraints as child tags or a list inside one tag).
