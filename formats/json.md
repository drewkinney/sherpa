keywords: json
best_for: API calls, function-calling schemas, config-driven agent tools

Same slots as keys in a single well-formed object. Use arrays for multi-item slots (constraints, examples). No comments — invalid JSON. If something needs a note that doesn't fit a slot, put it in a "notes" key rather than breaking syntax.
